---
import Layout from '../layouts/Layout.astro';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---

<Layout
  title="加入貢獻 | 嘉義國本學堂"
  description="歡迎加入嘉義國本學堂！無論你是農業專家、學生、或熱愛分享知識的社群成員，都能以多種方式貢獻內容。"
>
  <main class="contribute-page">
    <!-- Hero -->
    <section class="hero">
      <div class="container">
        <h1>加入我們</h1>
        <p class="tagline">共同構建開源農業知識平台</p>
      </div>
    </section>

    <!-- Contribution Ways -->
    <section class="section contribution-ways">
      <div class="container">
        <h2>你可以這樣貢獻</h2>
        <div class="ways-grid">
          <div class="way-card">
            <div class="way-icon">✍️</div>
            <h3>撰寫新文章</h3>
            <p>分享你的農業知識、技術心得或案例經驗，幫助更多人學習。</p>
          </div>
          <div class="way-card">
            <div class="way-icon">🔍</div>
            <h3>改進現有文章</h3>
            <p>修正錯誤、補充案例、優化結構，讓內容更完善。</p>
          </div>
          <div class="way-card">
            <div class="way-icon">🐛</div>
            <h3>回報問題</h3>
            <p>發現內容錯誤或網站問題？直接開 Issue 告訴我們。</p>
          </div>
          <div class="way-card">
            <div class="way-icon">💭</div>
            <h3>提出建議</h3>
            <p>建議新的主題、分類或功能，幫助我們改進平台。</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Quick Start -->
    <section class="section quick-start">
      <div class="container">
        <h2>快速開始</h2>
        <div class="steps">
          <div class="step">
            <div class="step-number">1</div>
            <div class="step-content">
              <h4>Fork 本 Repository</h4>
              <p>
                前往
                <a href="https://github.com/ahnchen1983/agrischlchiayi" target="_blank">
                  GitHub 專案頁面
                </a>
                ，點擊 <code>Fork</code> 按鈕，將專案複製到自己的帳號。
              </p>
            </div>
          </div>

          <div class="step">
            <div class="step-number">2</div>
            <div class="step-content">
              <h4>選擇內容分類</h4>
              <p>
                打開 <code>knowledge/</code> 目錄，選擇相應的分類資料夾：
              </p>
              <ul class="categories-list">
                <li><code>Agri-Basics/</code> — 農業基礎入門</li>
                <li><code>Agri-Advanced/</code> — 農業進階實務</li>
                <li><code>Farm-Management/</code> — 農場經營管理</li>
                <li><code>Crop-Production/</code> — 作物生產技術</li>
                <li><code>Facility-Farming/</code> — 設施農業</li>
                <li><code>Smart-Farming/</code> — 智慧農業</li>
                <li><code>Agri-Marketing/</code> — 農產品銷售</li>
                <li><code>Grants-Planning/</code> — 計畫撰寫補助</li>
                <li><code>Field-Visits/</code> — 場域交流觀摩</li>
                <li><code>Livestock-Health/</code> — 畜牧健康</li>
              </ul>
            </div>
          </div>

          <div class="step">
            <div class="step-number">3</div>
            <div class="step-content">
              <h4>新增或編輯 Markdown 檔案</h4>
              <p>
                建立或編輯 <code>.md</code> 檔案，遵循以下 Frontmatter 模板：
              </p>
              <div class="code-block">
                <pre>---
title: '文章標題'
description: '一句話摘要（50 字以內）'
date: 2026-04-18
tags: [國本學堂, 相關標籤]
subcategory: '子分類名稱'
level: '初階'
source: '資料來源'
featured: false
status: 'published'
---

# 文章內容
從這裡開始寫你的內容...</pre>
              </div>
            </div>
          </div>

          <div class="step">
            <div class="step-number">4</div>
            <div class="step-content">
              <h4>測試與驗證</h4>
              <p>在本地執行以下命令驗證文章：</p>
              <div class="code-block">
                <pre>npm install
npm run test
npm run build</pre>
              </div>
              <p>確認沒有錯誤後再提交 PR。</p>
            </div>
          </div>

          <div class="step">
            <div class="step-number">5</div>
            <div class="step-content">
              <h4>提交 Pull Request</h4>
              <p>
                將變更 Push 到你的 Fork，然後開啟 Pull Request。
                在 PR 描述中簡述：
              </p>
              <ul>
                <li>文章主題與內容概要</li>
                <li>為什麼這個主題很重要</li>
                <li>是否有相關參考資料或來源</li>
              </ul>
            </div>
          </div>

          <div class="step">
            <div class="step-number">6</div>
            <div class="step-content">
              <h4>維護者審核與發布</h4>
              <p>
                維護者會審查你的 PR，可能會提出建議或要求修改。
                通過審核後，你的文章就會被合併到主分支並自動發布。
              </p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Content Guidelines -->
    <section class="section guidelines">
      <div class="container">
        <h2>內容準則</h2>
        <div class="guidelines-grid">
          <div class="guideline-card">
            <h4>📍 與嘉義農業相關</h4>
            <p>優先涵蓋嘉義地區的農業實務、作物、技術與經營模式。不排斥全台灣或國際案例，但應與嘉義情況對應。</p>
          </div>
          <div class="guideline-card">
            <h4>🎯 實用導向</h4>
            <p>內容應具備實踐價值。避免過度理論化；盡可能舉例、提供步驟、分享技巧。</p>
          </div>
          <div class="guideline-card">
            <h4>📚 標明來源</h4>
            <p>引用他人研究、數據或經驗時，務必註明出處。使用腳註或參考資料欄位。</p>
          </div>
          <div class="guideline-card">
            <h4>📝 繁體中文</h4>
            <p>使用繁體中文撰寫。專有名詞可保留原文（如英文農業術語），但需在首次出現時附註。</p>
          </div>
          <div class="guideline-card">
            <h4>🔗 跨文章連結</h4>
            <p>使用 <code>[[文章標題]]</code> 語法建立連結，例如 <code>[[土壤基礎知識]]</code>。</p>
          </div>
          <div class="guideline-card">
            <h4>🖼️ 圖片與媒體</h4>
            <p>圖片應遵循 CC 授權。如不確定授權狀態，請避免使用或在文章中說明。</p>
          </div>
        </div>
      </div>
    </section>

    <!-- Frontmatter Template -->
    <section class="section frontmatter">
      <div class="container">
        <h2>Frontmatter 欄位說明</h2>
        <div class="fields">
          <div class="field">
            <h4><code>title</code> ✅ 必填</h4>
            <p>文章標題，簡潔清晰，避免超過 60 個字元。</p>
            <div class="example">例：「小番茄栽培技術：從育苗到採收」</div>
          </div>

          <div class="field">
            <h4><code>description</code> ✅ 必填</h4>
            <p>一句話摘要，50 字以內，讓讀者快速了解文章內容。</p>
            <div class="example">例：「掌握小番茄從選種、育苗、定植到採收的完整流程。」</div>
          </div>

          <div class="field">
            <h4><code>date</code> ✅ 必填</h4>
            <p>發布或最後編輯日期，格式：<code>YYYY-MM-DD</code></p>
            <div class="example">例：<code>2026-04-18</code></div>
          </div>

          <div class="field">
            <h4><code>tags</code> ✅ 必填</h4>
            <p>標籤陣列，至少包含「國本學堂」，可添加相關主題。</p>
            <div class="example">例：<code>[國本學堂, 小番茄, 栽培技術]</code></div>
          </div>

          <div class="field">
            <h4><code>subcategory</code> ✅ 必填</h4>
            <p>子分類名稱，用來組織同類別下的文章。</p>
            <div class="example">例：「育苗技術」（在 Crop-Production 類別下）</div>
          </div>

          <div class="field">
            <h4><code>level</code> ✅ 必填</h4>
            <p>難度等級：<code>初階</code> / <code>進階</code> / <code>卓越</code></p>
            <div class="example">例：<code>進階</code></div>
          </div>

          <div class="field">
            <h4><code>source</code> ✅ 必填</h4>
            <p>內容來源，可為課程、書籍、研究、實務經驗等。</p>
            <div class="example">例：「嘉義國本學堂農業課程、農場實驗」</div>
          </div>

          <div class="field">
            <h4><code>featured</code> 選填</h4>
            <p>是否在分類首頁特別推薦。<code>true</code> / <code>false</code></p>
            <div class="example">預設：<code>false</code></div>
          </div>

          <div class="field">
            <h4><code>status</code> 選填</h4>
            <p>文章狀態：<code>draft</code> / <code>published</code> / <code>archived</code></p>
            <div class="example">預設：<code>published</code></div>
          </div>

          <div class="field">
            <h4><code>crop</code> 選填</h4>
            <p>相關作物陣列，用於作物相關文章。</p>
            <div class="example">例：<code>[小番茄, 百香果]</code></div>
          </div>

          <div class="field">
            <h4><code>tech</code> 選填</h4>
            <p>相關技術或主題陣列。</p>
            <div class="example">例：<code>[滴灌系統, 環控技術]</code></div>
          </div>

          <div class="field">
            <h4><code>instructor</code> 選填</h4>
            <p>課程講師或內容貢獻者名稱。</p>
            <div class="example">例：「陳建築、王農業」</div>
          </div>

          <div class="field">
            <h4><code>courseDate</code> 選填</h4>
            <p>課程日期，格式：<code>YYYY-MM-DD</code></p>
            <div class="example">例：<code>2025-03-15</code></div>
          </div>
        </div>
      </div>
    </section>

    <!-- Review Process -->
    <section class="section review">
      <div class="container">
        <h2>審核流程</h2>
        <div class="review-timeline">
          <div class="timeline-item">
            <div class="timeline-marker">📝</div>
            <div class="timeline-content">
              <h4>提交 PR</h4>
              <p>你的 PR 被提交到我們的 GitHub repo。</p>
            </div>
          </div>

          <div class="timeline-item">
            <div class="timeline-marker">🔍</div>
            <div class="timeline-content">
              <h4>格式驗證</h4>
              <p>自動工具檢查 Frontmatter、Markdown 格式與內容品質。</p>
            </div>
          </div>

          <div class="timeline-item">
            <div class="timeline-marker">👥</div>
            <div class="timeline-content">
              <h4>人工審核</h4>
              <p>維護者逐字審查內容、準確性與適當性。</p>
            </div>
          </div>

          <div class="timeline-item">
            <div class="timeline-marker">💬</div>
            <div class="timeline-content">
              <h4>反饋與迭代</h4>
              <p>如有需要，我們會提出修改建議或要求澄清。</p>
            </div>
          </div>

          <div class="timeline-item">
            <div class="timeline-marker">✅</div>
            <div class="timeline-content">
              <h4>合併與發布</h4>
              <p>通過審核後，PR 被合併，文章自動發布到網站。</p>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Help & Support -->
    <section class="section help">
      <div class="container">
        <h2>需要幫助？</h2>
        <div class="help-cards">
          <div class="help-card">
            <h4>📚 查看範例</h4>
            <p>
              瀏覽 <code>knowledge/</code> 內現有的文章，了解格式與風格。
            </p>
          </div>

          <div class="help-card">
            <h4>💌 聯絡我們</h4>
            <p>
              有問題或想討論想法？歡迎開啟
              <a href="https://github.com/ahnchen1983/agrischlchiayi/discussions" target="_blank">
                GitHub Discussions
              </a>
              。
            </p>
          </div>

          <div class="help-card">
            <h4>📖 閱讀 CONTRIBUTING.md</h4>
            <p>
              詳細的貢獻指南請見
              <a href="https://github.com/ahnchen1983/agrischlchiayi/blob/main/CONTRIBUTING.md" target="_blank">
                專案 README
              </a>
              。
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="section cta">
      <div class="container">
        <h2>準備好貢獻了嗎？</h2>
        <p class="cta-text">
          無論你是農業專家、學生或熱愛分享的社群成員，你的貢獻都很重要！
        </p>
        <div class="cta-buttons">
          <a href="https://github.com/ahnchen1983/agrischlchiayi" target="_blank" class="btn btn-primary">
            造訪 GitHub 專案
          </a>
          <a href={`${base}/`} class="btn btn-secondary">回到首頁</a>
        </div>
      </div>
    </section>
  </main>
</Layout>

<style>
  .contribute-page {
    background: #fff;
  }

  .container {
    max-width: 900px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .hero {
    background: linear-gradient(135deg, #1a472a 0%, #2d5a3d 100%);
    color: white;
    padding: 6rem 2rem;
    text-align: center;
  }

  .hero h1 {
    font-size: clamp(2rem, 6vw, 3.5rem);
    margin: 0 0 1rem 0;
    font-weight: 700;
  }

  .tagline {
    font-size: 1.3rem;
    opacity: 0.95;
    max-width: 600px;
    margin: 0 auto;
    line-height: 1.5;
  }

  .section {
    padding: 4rem 2rem;
  }

  .section h2 {
    font-size: 2rem;
    margin-bottom: 2.5rem;
    text-align: center;
    color: #1a1a2e;
  }

  /* Contribution Ways */
  .ways-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 2rem;
  }

  .way-card {
    background: #f9fafb;
    padding: 2rem;
    border-radius: 12px;
    text-align: center;
    border-top: 4px solid #1a472a;
  }

  .way-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
  }

  .way-card h3 {
    color: #1a1a2e;
    margin: 0.5rem 0;
  }

  .way-card p {
    color: #666;
    margin: 0;
    font-size: 0.95rem;
  }

  /* Steps */
  .steps {
    display: grid;
    gap: 2rem;
  }

  .step {
    display: grid;
    grid-template-columns: 60px 1fr;
    gap: 1.5rem;
    padding: 1.5rem;
    background: #f9fafb;
    border-radius: 12px;
    border-left: 4px solid #1a472a;
  }

  .step-number {
    background: #1a472a;
    color: white;
    width: 60px;
    height: 60px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 1.5rem;
  }

  .step-content h4 {
    margin: 0 0 0.75rem 0;
    color: #1a1a2e;
  }

  .step-content p {
    margin: 0 0 0.75rem 0;
    color: #4b5563;
    line-height: 1.6;
  }

  .step-content p:last-child {
    margin-bottom: 0;
  }

  .categories-list {
    margin: 0.75rem 0;
    padding-left: 1.5rem;
  }

  .categories-list li {
    margin: 0.5rem 0;
    color: #666;
  }

  .code-block {
    background: #1a1a2e;
    color: #e0e0e0;
    padding: 1rem;
    border-radius: 8px;
    overflow-x: auto;
    margin: 0.75rem 0;
  }

  .code-block pre {
    margin: 0;
    font-family: monospace;
    font-size: 0.9rem;
    line-height: 1.5;
  }

  /* Guidelines */
  .guidelines-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .guideline-card {
    background: #fef8f3;
    padding: 1.5rem;
    border-radius: 12px;
    border-left: 4px solid #d4a574;
  }

  .guideline-card h4 {
    margin: 0 0 0.75rem 0;
    color: #1a1a2e;
  }

  .guideline-card p {
    margin: 0;
    color: #666;
    line-height: 1.6;
  }

  /* Frontmatter Fields */
  .fields {
    display: grid;
    gap: 1.5rem;
  }

  .field {
    background: #f9fafb;
    padding: 1.5rem;
    border-radius: 8px;
    border-left: 4px solid #1a472a;
  }

  .field h4 {
    margin: 0 0 0.5rem 0;
    color: #1a1a2e;
    font-family: monospace;
  }

  .field p {
    margin: 0 0 0.75rem 0;
    color: #4b5563;
    line-height: 1.6;
  }

  .example {
    background: #1a1a2e;
    color: #90ee90;
    padding: 0.75rem 1rem;
    border-radius: 4px;
    font-family: monospace;
    font-size: 0.9rem;
    margin: 0;
  }

  /* Review Timeline */
  .review-timeline {
    display: grid;
    gap: 1.5rem;
    max-width: 600px;
    margin: 0 auto;
  }

  .timeline-item {
    display: grid;
    grid-template-columns: 50px 1fr;
    gap: 1.5rem;
    align-items: flex-start;
  }

  .timeline-marker {
    font-size: 2rem;
  }

  .timeline-content h4 {
    margin: 0 0 0.5rem 0;
    color: #1a1a2e;
  }

  .timeline-content p {
    margin: 0;
    color: #666;
  }

  /* Help Cards */
  .help-cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 2rem;
  }

  .help-card {
    background: #f9fafb;
    padding: 1.5rem;
    border-radius: 12px;
  }

  .help-card h4 {
    color: #1a1a2e;
    margin: 0 0 0.75rem 0;
  }

  .help-card p {
    color: #666;
    margin: 0;
    line-height: 1.6;
  }

  .help-card a {
    color: #1a472a;
    text-decoration: underline;
  }

  /* CTA */
  .cta {
    background: #f9fafb;
    text-align: center;
  }

  .cta h2 {
    margin-bottom: 1rem;
  }

  .cta-text {
    font-size: 1.1rem;
    color: #666;
    margin-bottom: 2rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-block;
    padding: 0.75rem 1.5rem;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.2s;
    border: none;
    cursor: pointer;
  }

  .btn-primary {
    background: #1a472a;
    color: white;
  }

  .btn-primary:hover {
    background: #0f2e1a;
  }

  .btn-secondary {
    background: white;
    color: #1a472a;
    border: 2px solid #1a472a;
  }

  .btn-secondary:hover {
    background: #1a472a;
    color: white;
  }

  code {
    background: #e5e7eb;
    padding: 0.2em 0.4em;
    border-radius: 4px;
    font-family: monospace;
  }

  @media (max-width: 640px) {
    .section {
      padding: 2.5rem 1rem;
    }

    .section h2 {
      font-size: 1.5rem;
    }

    .step {
      grid-template-columns: 1fr;
    }

    .step-number {
      width: 50px;
      height: 50px;
      font-size: 1.2rem;
    }

    .cta-buttons {
      flex-direction: column;
    }

    .btn {
      width: 100%;
      text-align: center;
    }
  }
</style>
